# Variáveis para o backend (local ou deploy remoto)
# Copie para `.env` na raiz do monorepo.

NODE_ENV=development
PORT=3001

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=

ALLOWED_ORIGINS=
ALLOWED_ORIGIN_SUFFIXES=

AUTH_COOKIE_SAME_SITE=

TRUST_PROXY_HOPS=1

# REDIS_URL=
